import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    marginTop: 130,
  },
  texto: {
    textAlign: 'center',
    color: 'white',
    fontSize: 35,
    fontStyle: 'italic',
    marginTop: 50,
  },
});

export default styles;
